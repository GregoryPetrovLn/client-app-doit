import React from 'react';
import './Loader.scss'
const Loader = () => {
    return (
        <div className={'loader'}>
            <span>...loading</span>
        </div>
    );
};

export default Loader;