import React from 'react';
import './Header.scss';

const Header = () => {
  return <div className={'header-wrapper'}>
    <span className={'header-wrapper__title'}>DOIT</span>
  </div>;
};

export default Header;
